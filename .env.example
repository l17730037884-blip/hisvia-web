# ===== HISVIA 环境变量 =====
# 部署到 Vercel 时，在 Project Settings → Environment Variables 中配置以下变量

# 站点 URL（用于 sitemap.xml / robots.txt / SEO canonical）
NEXT_PUBLIC_SITE_URL=https://hisvia.com

# 后端 API 地址（预留，当前前端无实际调用；后端就绪后指向 api.hisvia.com）
NEXT_PUBLIC_API_BASE_URL=https://api.hisvia.com
